#include <iostream>
#include <stdio.h>

using namespace std;

int main() {
    long long int a,b,c;
    cin >> a >> b >> c;

    long long int sum = (long long int)a + (long long int)b + (long long int)c;
    printf("%lld\n", sum);

    return 0;
}